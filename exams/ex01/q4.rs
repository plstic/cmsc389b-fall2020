fn checkarray(lst :&[i32], co1 :i32, co2 :i32){
	//lst is the integer list
	// co1 is the coefficient of the a_{n-1} term of the linear combination
	// co2 is the coefficient of the a_{n-2} term of the linear combination
	return false;
}

fn main(){
	checkarray([1,1,2,3,5,8], 1, 1);
}
